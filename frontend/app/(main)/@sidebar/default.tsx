'use client';

/**
 * Default sidebar slot — renders nothing for routes
 * that don't define their own sidebar (e.g., /account, /users).
 */
export default function DefaultSidebar() {
  return null;
}
